-- SUS-SP editorial refinement after strict R1 audit
-- Keeps the recovered board identity and restores useful generator principles from contract 2.1.
-- Scientific source strategy remains unchanged.

-- Identity preserved:
-- * predominantly clinical cases;
-- * medium, objective stems;
-- * strong practical-decision focus;
-- * functional urgency / ward / outpatient / APS-SUS contexts;
-- * diagnosis, immediate management, referral and prevention;
-- * small conceptual/pathophysiology subset;
-- * short-to-medium alternatives.

-- Generator refinements:
-- * easy/direct items may stay direct;
-- * medium/hard items require real integration, not just recognition + protocol recall;
-- * add 1-2 functional clinical/lab data points when they change diagnosis, severity, indication, timing or priority;
-- * medium/hard items need at least one plausible competitor after full vignette review;
-- * restore contract-2.1 principles: same-axis distractors, surface-elimination check, relevance of every highlighted datum, classic-pattern gate, structural-variety gate;
-- * difficulty comes from reasoning + alternative competition;
-- * lot pre-flight checks difficulty variety, cognitive-operation concentration and real answer-position distribution;
-- * no rigid quotas for APS/SUS, difficulty or step count.

-- Strict R1 result retained as calibration history:
-- FINAL_PROMPT_SCORE = 76/100 (FAIL)
-- Science/key: 15/15 concordance, 0 scientific failures, 0 SBA failures.
